package ohnosequences.api.rnacentral.test

import org.scalatest.FunSuite

import ohnosequences.api.rnacentral._
import java.io.File

class Parsing extends FunSuite {

  ignore("parse all entries") {

    println(s"Number of parsed entries: ${(Entries entriesFrom testData).length}")
  }

  test("data is aligned") {}
}